import { PageHeader } from "@/components/dashboard/page-header"

/**
 * Placeholder so the five `/…/assets` routes build and render something.
 * Replaced by Task 16's register table and asset detail view — this stub is
 * not the final component.
 */
export function AssetPage() {
  return (
    <div className="flex flex-col gap-6">
      <PageHeader
        kicker="Assets"
        title="Asset register"
        sub="Coming soon — custody, requests and repairs will live here."
      />
    </div>
  )
}
