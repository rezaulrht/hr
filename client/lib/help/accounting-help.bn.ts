import type { HelpOverlay } from "./types"

/** Partial by design. An untranslated page renders entirely in English —
 *  a half-translated paragraph is worse than an untranslated one. */
export const HELP_BN: HelpOverlay = {}
